// import ParticleField from 'react-particles-webgl';

// import { addParticlesPanel, addDebug } from '../Utils/Debug';

import BackgroundConfig from '../Config/particles3D.js';

export default function Background3D({particlesContainer}){
    const config = BackgroundConfig;
    return <>
        {/* <ParticleField  id={particlesContainer} config={config} /> */}
    </>;
};
